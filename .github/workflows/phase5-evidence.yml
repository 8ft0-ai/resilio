name: Phase 5 product evidence caller

on:
  workflow_dispatch:
    inputs:
      build_id:
        description: Exact successful Phase 5 Cloud Build ID
        required: true
        type: string

permissions:
  contents: read

jobs:
  evidence:
    permissions:
      contents: read
      issues: write
      id-token: write
    uses: 8ft0-ai/resilio/.github/workflows/phase5-evidence-reusable.yml@47b3b17d32ffebf3ce8e9b7d15bc3d3539dc7239
    with:
      build_id: ${{ inputs.build_id }}
