name: Phase 5 product evidence caller

on:
  workflow_dispatch:
    inputs:
      build_id:
        description: Exact successful Phase 5 Cloud Build ID
        required: true
        type: string

permissions:
  contents: read

jobs:
  evidence:
    permissions:
      contents: read
      issues: write
      id-token: write
    uses: 8ft0-ai/resilio/.github/workflows/phase5-evidence-reusable.yml@9513cd2a93241a8f870d85b4261102327574da5b
    with:
      build_id: ${{ inputs.build_id }}
