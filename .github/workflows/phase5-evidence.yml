name: Phase 5 product evidence caller

on:
  workflow_dispatch:
    inputs:
      build_id:
        description: Exact successful Phase 5 Cloud Build ID
        required: true
        type: string

permissions:
  contents: read

jobs:
  evidence:
    permissions:
      contents: read
      issues: write
      id-token: write
    uses: 8ft0-ai/resilio/.github/workflows/phase5-evidence-reusable.yml@67e61a6c0d4930f8a5c5db545ef84d8430e437bc
    with:
      build_id: ${{ inputs.build_id }}
